import "./Header.css";
import "../App.css";

const Header = () => {
  return (
    <header className="header">
      <img className="logo" src="../assets/Logo.svg" alt="Logo da página" />
    </header>
  );
};

export default Header;